var h1El = document.querySelector('h1')

function index() {
  h1El.textContent = 'Now we are home'
}

function about () {
  h1El.textContent = 'Now we are about'
}

function epicfail() {
  h1El.textContent = 'You need to update java'
}
